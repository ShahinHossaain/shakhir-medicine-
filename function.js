const getQuerySelector = (name) => {
    return document.querySelector(name);
}
const getQuerySelectorAll = (name) => {
    return document.querySelectorAll(name);
}
const del = (e) => {
    console.log(e.target)
}